// Placeholder: the synthetic-data dry-run harness moved to
// tools/verify_synthetic_marg_import.mts at the repo root. This stub remains
// because the sandbox cannot unlink files on the Windows mount; safe to delete
// from PowerShell side. Kept inert by re-exporting nothing.
export {};
