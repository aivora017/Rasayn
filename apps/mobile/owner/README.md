# @pharmacare/mobile-owner

**Owner App** — KPI dashboard, AI Copilot, Digital Twin, multi-store oversight, push alerts.

## Status
**SCAFFOLD** — Expo SDK 52 + React Native 0.76 + Reanimated 3.16. Ready for `expo install` and EAS build.

## Setup
```sh
cd apps/mobile/owner
npm install
npx expo start
```

## Build
```sh
eas build --platform android --profile preview
eas build --platform ios --profile preview
```
